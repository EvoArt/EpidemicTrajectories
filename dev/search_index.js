var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"section","location":"api/#High-level-entry-point","page":"API","text":"","title":"High-level entry point"},{"category":"section","location":"api/#State-spaces","page":"API","text":"","title":"State spaces"},{"category":"section","location":"api/#Rate-functions","page":"API","text":"","title":"Rate functions"},{"category":"section","location":"api/#Simulation","page":"API","text":"","title":"Simulation"},{"category":"section","location":"api/#Likelihood","page":"API","text":"","title":"Likelihood"},{"category":"section","location":"api/#Latent-state-sampling","page":"API","text":"","title":"Latent-state sampling"},{"category":"section","location":"api/#Transition-matrix-models","page":"API","text":"","title":"Transition-matrix models"},{"category":"function","location":"api/#EpidemicTrajectories.epidemic_model","page":"API","text":"epidemic_model(state_space, rates; tests=(), coupling=true) -> EpidemicModel\n\nBundle a discrete-time epidemic model's fixed structure — the StateSpace, the RateBundle (e.g. TwoStateSI or an EpiTransitionMatrix), and any diagnostic DiagnosticTests — and return an EpidemicModel. Pair it with a build_data object and call the returned closures in the (model, X, data) vocabulary.\n\ncoupling is passed to the iFFBS sampler (include the between-individual coupling term; default true).\n\nExample\n\nem = epidemic_model(SI, TwoStateSI(); tests = (rams,))\ndata = build_data(em, group; observations = (Rmask,), initial_prob = init)\n\n# in a PPL model body, after `X ~ TrajectoryLatent(...)`:\n@addlogprob! em.loglik((; α, β, m), X, data)\n\n\n\n\n\n","title":"EpidemicTrajectories.epidemic_model"},{"category":"type","location":"api/#EpidemicTrajectories.EpidemicModel","page":"API","text":"EpidemicModel\n\nReturned by epidemic_model. Holds a model's fixed structure (state space, rate bundle, diagnostic tests) and exposes three callables in the (model, X, data) vocabulary — model is the parameters, X the latent trajectory, data an EpidemicData:\n\nloglik(model, X, data) -> Real — log-likelihood of trajectory X. Autodiff-friendly in model; X and data are fixed. Add to a PPL's log density, e.g. @addlogprob! em.loglik(pars, X, data).\nsimulate(rng, model, data) -> (states, observations) — draw a hidden trajectory and observed test results.\nlatent!(rng, model, X, data) -> X — one iFFBS Gibbs sweep, resampling X in place (and updating any bookkeeping in data).\n\n\n\n\n\n","title":"EpidemicTrajectories.EpidemicModel"},{"category":"function","location":"api/#EpidemicTrajectories.build_data","page":"API","text":"build_data(em::EpidemicModel, group; observations=(), initial_prob=nothing,\n           n_times=nothing) -> EpidemicData\n\nBuild the data object for a model: the observations the PPL is conditioned on plus the fixed group structure (and room for any per-sweep bookkeeping a latent update maintains). Built once and passed to em.loglik/em.simulate/em.latent!.\n\ngroup: group/pen index per individual (length n_ind).\nobservations: tuple of result matrices aligned with the model's tests, each n_ind × n_times, entries -1 (missing) / 0 / 1.\ninitial_prob: length-nstates probability vector over states at t = 1, or nothing to supply per call.\nn_times: inferred from observations; pass explicitly if observations is empty.\n\n\n\n\n\n","title":"EpidemicTrajectories.build_data"},{"category":"type","location":"api/#EpidemicTrajectories.EpidemicData","page":"API","text":"EpidemicData\n\nThe data object in the (model, X, data) split: the observations a PPL model is conditioned on, plus the fixed group structure and any mutable per-sweep bookkeeping a latent update maintains. Built by build_data; passed to the EpidemicModel closures. Distinct from the latent trajectory X (passed separately) and from model (the parameters).\n\nFields:\n\ngroup — group/pen index per individual.\nmembers — members(g) gives the individuals in group g.\nobservations — tuple of diagnostic-test result matrices (aligned with the model's tests), each n_ind × n_times, entries -1/0/1.\ninitial_prob — probability vector over states at t = 1 (or nothing, then supplied per call).\nn_ind, n_times.\n\n\n\n\n\n","title":"EpidemicTrajectories.EpidemicData"},{"category":"type","location":"api/#EpidemicTrajectories.StateSpace","page":"API","text":"StateSpace(codes; names=nothing)\n\nA discrete state space for an individual-level epidemic model. codes is the tuple of integer state codes in the fixed order they index transition-probability matrices (so codes[1] is dense index 1, codes[2] is dense index 2, ...). names is an optional tuple of Symbols for pretty-printing / clarity.\n\nThe order in codes IS the dense ordering used everywhere internally — a transition matrix P produced by the model has P[a, b] = Prob(dense state a at t -> dense state b at t+1), where dense index a corresponds to codes[a].\n\nExamples\n\nSI   = StateSpace((0, 1);       names=(:S, :I))         # susceptible / infected\nSIS  # same two states, recurrent dynamics (S<->I) — encoding identical to SI\nSEID = StateSpace((0, 3, 1, 9); names=(:S, :E, :I, :D)) # badger encoding\n\n\n\n\n\n","title":"EpidemicTrajectories.StateSpace"},{"category":"constant","location":"api/#EpidemicTrajectories.SI","page":"API","text":"SI\n\nThe canonical two-state susceptible/infected state space, StateSpace((0, 1); names=(:S, :I)). This is the state space of the iFFBS-paper cattle E. coli model: an animal is either susceptible (X=0) or infected (X=1), with recurrent S<->I dynamics (an infected animal can recover back to susceptible and be re-infected — there is NO recovered/removed compartment). SIS is provided as an alias to make the recurrent dynamics explicit at the call site; the state encoding is identical.\n\n\n\n\n\n","title":"EpidemicTrajectories.SI"},{"category":"constant","location":"api/#EpidemicTrajectories.SIS","page":"API","text":"SIS\n\nAlias for SI — the same two-state {S, I} encoding, named to make the recurrent (susceptible-infected-susceptible) dynamics explicit. There is no separate recovered compartment; recovery returns an individual to S.\n\n\n\n\n\n","title":"EpidemicTrajectories.SIS"},{"category":"constant","location":"api/#EpidemicTrajectories.SEID","page":"API","text":"SEID\n\nThe badger bovine-TB state space StateSpace((0, 3, 1, 9); names=(:S, :E, :I, :D)): susceptible, exposed (latently infected, not yet infectious), infectious, and dead/removed. The sparse codes match the external capture-recapture data pipeline. Provided for the individual-level SEID milestone; the two-state SI model is the first supported case.\n\n\n\n\n\n","title":"EpidemicTrajectories.SEID"},{"category":"function","location":"api/#EpidemicTrajectories.nstates","page":"API","text":"nstates(ss::StateSpace) -> Int\n\nNumber of distinct states (dense dimension of the transition matrices).\n\n\n\n\n\n","title":"EpidemicTrajectories.nstates"},{"category":"function","location":"api/#EpidemicTrajectories.state_index","page":"API","text":"state_index(ss::StateSpace, code::Integer) -> Int\n\nDense index (1-based) of the user state code within ss.codes. Errors if code is not one of the state space's codes.\n\n\n\n\n\n","title":"EpidemicTrajectories.state_index"},{"category":"type","location":"api/#EpidemicTrajectories.RateBundle","page":"API","text":"RateBundle\n\nAbstract supertype for a functional model specification. A concrete subtype must implement\n\ntransition_matrix_at(rb, pars, model, data, i, t) -> AbstractMatrix\n\nreturning the per-individual, per-step transition-probability matrix P with P[a, b] = Prob(state a at t -> state b at t+1) in the dense state ordering of the model's StateSpace. pars is whatever parameter object the model uses (a NamedTuple is typical); model and data are user-defined carriers of the fixed structure and observations; i, t are the individual and time indices.\n\nThe returned matrix's element type should follow eltype(pars) so the same function differentiates cleanly under AD (for the HMC likelihood) and runs in plain Float64 (for the FFBS sampler) — see TwoStateSI for the canonical implementation.\n\n\n\n\n\n","title":"EpidemicTrajectories.RateBundle"},{"category":"function","location":"api/#EpidemicTrajectories.transition_matrix_at","page":"API","text":"transition_matrix_at(rb::RateBundle, pars, model, data, i, t) -> AbstractMatrix\n\nThe one method every RateBundle must implement. See RateBundle's docstring for the contract. Must be defined for each concrete bundle type.\n\n\n\n\n\n","title":"EpidemicTrajectories.transition_matrix_at"},{"category":"type","location":"api/#EpidemicTrajectories.TwoStateSI","page":"API","text":"TwoStateSI(; foi_external, foi_within, recovery)\n\nThe functional-style rate bundle for the two-state {S, I} recurrent Markov model of the iFFBS paper (Touloupou et al. 2019). Each animal in a pen is either susceptible (S) or infected (I); transitions from t to t+1 are:\n\nS -> I with probability 1 - exp(-α - β·I₋), where I₋ is the number of OTHER infected animals in the same pen at time t (frequency/density-dependent within-pen transmission), α the external (primary) force of infection, and β the within-pen transmission rate.\nI -> S (recovery) with probability 1/m, where m is the mean infectious period; I -> I (stay infected) with probability 1 - 1/m. There is no removed compartment — a recovered animal is susceptible again.\n\nThe three fields are functions extracting the relevant scalar from pars (they default to reading NamedTuple fields α, β, m):\n\nfoi_external(pars) -> α\nfoi_within(pars)   -> β\nrecovery(pars)     -> 1/m  (returns the per-step recovery PROBABILITY)\n\ntransition_matrix_at(::TwoStateSI, pars, model, data, i, t) needs data to expose, for the pen/group containing individual i:\n\ndata.states::AbstractArray{<:Integer} — the current hidden-state trajectory (used to count other infected animals I₋ at time t);\ndata.group[i] / data.members(data, g) — group membership, so I₋ counts only same-pen animals and EXCLUDES i itself (leave-one-out force of infection, the key to the FFBS coupling term).\n\nSee the package docs' iFFBS example for a concrete data object.\n\n\n\n\n\n","title":"EpidemicTrajectories.TwoStateSI"},{"category":"type","location":"api/#EpidemicTrajectories.DiagnosticTest","page":"API","text":"DiagnosticTest(; sensitivity, specificity=(pars -> 1.0), positive_code=1)\n\nAn imperfect diagnostic test applied to individuals over time. sensitivity(pars) returns P(test positive | truly infected); specificity(pars) returns P(test negative | truly susceptible) (default 1, i.e. no false positives, as in the iFFBS-paper cattle tests). positive_code is the infected state's user code.\n\nUsed to build the per-state observation likelihood via observation_likelihood: given a test result r for an individual at a time, it returns the vector of likelihoods [P(r | state a) for a in dense states], the \"corrector\" term the FFBS filter multiplies into its predicted state probabilities.\n\nA test result convention (matching the iFFBS-paper data): a NEGATIVE value (e.g. -1) means \"not tested / result missing\" and contributes likelihood 1 to every state (no information); 1 means test positive, 0 means test negative.\n\n\n\n\n\n","title":"EpidemicTrajectories.DiagnosticTest"},{"category":"function","location":"api/#EpidemicTrajectories.observation_likelihood","page":"API","text":"observation_likelihood(tests, pars, ss::StateSpace, results) -> Vector\n\nGiven a tuple/vector of DiagnosticTests and their results for one individual at one time (each result -1/missing, 0 negative, or 1 positive), return the length-nstates(ss) vector whose entry a is the joint likelihood of all the results if the individual were truly in dense state a. Tests are assumed conditionally independent given the true state.\n\nFor the two-state {S, I} model with a test of sensitivity θ and specificity 1: a positive result gives likelihood [0, θ] (impossible if susceptible, probability θ if infected); a negative gives [1, 1-θ]; a missing result gives [1, 1].\n\n\n\n\n\n","title":"EpidemicTrajectories.observation_likelihood"},{"category":"type","location":"api/#EpidemicTrajectories.EpiModel","page":"API","text":"EpiModel(state_space, rates)\n\nMinimal carrier bundling a StateSpace with a RateBundle. This is the model argument the simulate / likelihood / FFBS functions expect (they read model.state_space and model.rates). You can use any object with those two fields instead — EpiModel is just the convenient default.\n\n\n\n\n\n","title":"EpidemicTrajectories.EpiModel"},{"category":"function","location":"api/#EpidemicTrajectories.make_data","page":"API","text":"make_data(states, group; members=default_members)\n\nBuild the lightweight data object the package functions expect, from a hidden state matrix states (n_individuals x n_times, user state codes) and a group vector (group index per individual). members(data, g) defaults to \"all individuals whose group equals g\" (precomputed for speed). Additional fields (e.g. test results) can be attached by using a NamedTuple built with merge(make_data(...), (; test_results=...))-style extension, or by supplying your own object with the required fields.\n\n\n\n\n\n","title":"EpidemicTrajectories.make_data"},{"category":"function","location":"api/#EpidemicTrajectories.simulate_trajectory","page":"API","text":"simulate_trajectory(rng, state_space, rates, pars, group, initial_prob; n_times)\n\nConvenience wrapper: allocate a fresh states matrix and simulate a trajectory for the given parameters. Returns (states, data) where states is the n_individuals x n_times hidden-state matrix and data is the data object (so downstream likelihood/FFBS calls can reuse it). group gives each individual's group index.\n\nExample (two-state S/I, one pen of 8 animals, 99 days)\n\nss = SI\nrates = TwoStateSI()\npars = (; α=0.009, β=0.01, m=9.0)\ngroup = ones(Int, 8)                 # all 8 animals in pen 1\ninit = [0.9, 0.1]                    # 10% infected at t=1 (ν = 0.1)\nstates, data = simulate_trajectory(rng, ss, rates, pars, group, init; n_times=99)\n\n\n\n\n\n","title":"EpidemicTrajectories.simulate_trajectory"},{"category":"function","location":"api/#EpidemicTrajectories.simulate_observations","page":"API","text":"simulate_observations(rng, tests, pars, state_space, states) -> Vector{Matrix{Int}}\n\nGiven simulated hidden states and a tuple of DiagnosticTests, draw observed test results for every individual and time. Returns one n_individuals x n_times Int matrix of results per test, with 1 = positive, 0 = negative (an individual truly in the infected state tests positive with the test's sensitivity; a truly-uninfected individual tests positive with probability 1 - specificity). No missingness is introduced here — mask untested times afterward by setting entries to -1.\n\n\n\n\n\n","title":"EpidemicTrajectories.simulate_observations"},{"category":"function","location":"api/#EpidemicTrajectories.trajectory_loglik","page":"API","text":"trajectory_loglik(pars, model, data) -> Real\n\nLog-probability of the hidden state trajectory data.states given the parameters pars, under the model's per-step transition matrices. Sums log P[a→b] over every individual i and every time step t → t+1, where a, b are individual i's dense states at t and t+1.\n\nPure and autodiff-friendly: differentiates cleanly w.r.t. pars (the transition matrices follow eltype(pars)), treats data.states as constant. Drop it straight into a PracticalBayes @model via @addlogprob! trajectory_loglik(pars, model, data), or into any other PPL's \"add to the log density\" primitive.\n\nmodel.state_space and model.rates are read; data must expose .states, .group, .members (see make_data).\n\n\n\n\n\n","title":"EpidemicTrajectories.trajectory_loglik"},{"category":"function","location":"api/#EpidemicTrajectories.observation_loglik","page":"API","text":"observation_loglik(pars, model, data, tests, results) -> Real\n\nLog-likelihood of the observed diagnostic-test results given the fixed hidden state trajectory data.states. tests is a tuple of DiagnosticTests; results is a tuple/vector of n_individuals x n_times result matrices (one per test, entries -1 missing / 0 negative / 1 positive), aligned with tests.\n\nSums, over every individual and time, the log of the joint per-state observation likelihood evaluated at the individual's TRUE state (from data.states).\n\nUsually the test-sensitivity/specificity parameters have conjugate closed-form Gibbs updates (see the iFFBS-paper example) and are NOT sampled by HMC — in that case this term is constant w.r.t. the HMC parameters and can be omitted from the @addlogprob! HMC target. It is provided for completeness and for models that DO put the test parameters through HMC.\n\n\n\n\n\n","title":"EpidemicTrajectories.observation_loglik"},{"category":"function","location":"api/#EpidemicTrajectories.ffbs_individual!","page":"API","text":"ffbs_individual!(rng, model, data, i, tests, results;\n                 initial_prob, coupling=true)\n\nResample individual i's full hidden-state trajectory in place into data.states[i, :], from its exact full conditional given the current parameters (model.pars), the observed results, and every other individual's current trajectory. Implements forward-filtering/backward-sampling with an imperfect-test observation likelihood and (optionally) the penmate COUPLING term.\n\nArguments:\n\nmodel: object with .state_space, .rates, and .pars (current parameter values — FFBS runs in plain Float64, never under AD).\ndata: exposes .states, .group, .members (see make_data).\ni: focal individual index.\ntests, results: tuple of DiagnosticTests and their result matrices (see observation_loglik for the format).\ninitial_prob: length-nstates probability vector over dense states at t=1.\ncoupling: include the penmate coupling likelihood (default true). Set false to reproduce the paper's no-coupling variant / for models where an individual's state does not enter others' transition rates.\n\nReturns the newly sampled dense-state vector for individual i.\n\n\n\n\n\n","title":"EpidemicTrajectories.ffbs_individual!"},{"category":"function","location":"api/#EpidemicTrajectories.ffbs_sweep!","page":"API","text":"ffbs_sweep!(rng, model, data, tests, results; initial_prob, coupling=true)\n\nOne full iFFBS Gibbs sweep of the entire latent state: resample every individual's trajectory in turn via ffbs_individual!, in index order (each resample conditions on the just-updated trajectories of earlier individuals — a systematic-scan Gibbs sweep). Mutates data.states in place and returns it.\n\nThis is the function a PracticalBayes AbstractLatentKernel's latent_step calls once per outer Gibbs sweep (see the PracticalEpiBayes companion package), handing back (; X = copy(data.states)).\n\n\n\n\n\n","title":"EpidemicTrajectories.ffbs_sweep!"},{"category":"type","location":"api/#EpidemicTrajectories.SimpleEpiTransitionMatrix","page":"API","text":"SimpleEpiTransitionMatrix(; state_space, transitions, rates)\n\nA compartmental discrete-time state-transition model whose transition rates are pure functions of the compartment counts: each element of rates is rate(pars, counts, t) -> per-capita per-step transition PROBABILITY, where counts is the current dense-state count vector and t the time step. transitions is a vector of (from_code, to_code) pairs; state_space is the StateSpace.\n\nThis is the exchangeable, count-sufficient case — use it for aggregate compartmental (chain-binomial) models. For transition probabilities that depend on individual identity/covariates/network structure, use EpiTransitionMatrix instead.\n\nUse with simulate_chain_binomial and chain_binomial_loglik. Build directly or via the @transitions macro.\n\n\n\n\n\n","title":"EpidemicTrajectories.SimpleEpiTransitionMatrix"},{"category":"type","location":"api/#EpidemicTrajectories.EpiTransitionMatrix","page":"API","text":"EpiTransitionMatrix(; state_space, transitions, rates)\n\nA discrete-time state-transition model whose transition rates are the FULL functional-style signature: each element of rates is rate(pars, model, data, i, t) -> per-step transition PROBABILITY for individual i at time t, so a transition probability can depend on individual identity, per-individual covariates, network/spatial structure, or history — everything the functional RateBundle style can express.\n\ntransitions is a vector of (from_code, to_code) pairs; state_space is the StateSpace. Because the rates carry the individual-level signature, an EpiTransitionMatrix is itself a RateBundle: it implements transition_matrix_at, so it works directly with trajectory_loglik, ffbs_sweep!, and the simulator — the same machinery as any hand-written functional bundle. Build directly or via the @transitions macro.\n\n\n\n\n\n","title":"EpidemicTrajectories.EpiTransitionMatrix"},{"category":"macro","location":"api/#EpidemicTrajectories.@transitions","page":"API","text":"@transitions state_space begin\n    S -> I = f\n    I -> S = g\n    ...\nend\n\nModelling-language front-end for building a transition-matrix model. Each line A -> B = rate declares a transition from state A to state B. The state names A, B must be the names of the supplied state_space (a StateSpace whose names are set); they are resolved to state codes automatically.\n\nThe rate is written as a bare expression referring to the rate-function arguments by name — no -> lambda needed. Those arguments are pars, counts, t for the default count-based style, and pars, model, data, i, t for the per-individual style. An explicit lambda is still accepted for rates that need a multi-line body.\n\nBy default this builds a SimpleEpiTransitionMatrix. Pass :individual as a leading argument to build an EpiTransitionMatrix instead, whose rates can depend on individual identity, covariates, or history.\n\nExamples\n\n# count-based (chain binomial): rate expressions see `pars`, `counts`, `t`\nsi = @transitions SI begin\n    S -> I = -expm1(-(pars.α + pars.β * counts[2]))\n    I -> S = 1 / pars.m\nend\n\n# per-individual: rate expressions see `pars`, `model`, `data`, `i`, `t`\nsi2 = @transitions :individual SI begin\n    S -> I = -expm1(-(pars.α + pars.β * count_infected_penmates(data, i, t)))\n    I -> S = 1 / pars.m\nend\n\n\n\n\n\n","title":"EpidemicTrajectories.@transitions"},{"category":"function","location":"api/#EpidemicTrajectories.incidence_matrix","page":"API","text":"incidence_matrix(m) -> Matrix{Int}\n\nThe nstates x ntransitions incidence (stoichiometry) matrix A: column r has -1 in the row of transition r's source state and +1 in the row of its destination state (dense-state ordering). Advancing counts by a per-transition event vector is counts_next = counts + A * events. Works for any model with state_space and transitions fields (SimpleEpiTransitionMatrix or EpiTransitionMatrix).\n\n\n\n\n\n","title":"EpidemicTrajectories.incidence_matrix"},{"category":"function","location":"api/#EpidemicTrajectories.simulate_chain_binomial","page":"API","text":"simulate_chain_binomial(rng, m::SimpleEpiTransitionMatrix, pars, counts0; n_times)\n\nSimulate a compartmental count trajectory forward n_times steps from an initial count vector counts0 (dense-state order). At each step, for each transition the number of individuals making it is Binomial(n_available_in_source, rate). When several transitions leave the same source compartment they compete (the source is depleted in transition order).\n\nReturns (counts, events): an nstates x n_times count matrix and an ntransitions x (n_times-1) realized-event-count matrix.\n\n\n\n\n\n","title":"EpidemicTrajectories.simulate_chain_binomial"},{"category":"function","location":"api/#EpidemicTrajectories.chain_binomial_loglik","page":"API","text":"chain_binomial_loglik(pars, m::SimpleEpiTransitionMatrix, counts, events) -> Real\n\nLog-likelihood of an observed compartmental trajectory under the chain-binomial model: for each step and transition, logpdf(Binomial(n_source, rate), events[r,t]). Pure and autodiff-friendly (differentiates w.r.t. pars; counts/events are constants) — drop into a PracticalBayes @model via @addlogprob! chain_binomial_loglik(pars, m, counts, events).\n\nThis is the transition-matrix style's analogue of trajectory_loglik; for the same underlying process both target the same parameters (aggregate counts vs per-individual trajectories) and recover the same values.\n\n\n\n\n\n","title":"EpidemicTrajectories.chain_binomial_loglik"},{"category":"section","location":"tutorials/cattle_iffbs/#Fitting-a-latent-trajectory-epidemic-model","page":"Fitting a model with a PPL","text":"In this tutorial we simulate capture-recapture data from a two-state susceptible/infected epidemic and recover the model parameters, using EpidemicTrajectories.jl for the epidemic model and PracticalBayes.jl for inference.\n\nThe model is the two-state model of Touloupou et al. (2019): each animal is either susceptible or infected, and moves between the two states at each time step. There is no recovered compartment — an infected animal recovers back to susceptible and can be reinfected. We observe an imperfect diagnostic test on a subset of days, and we never observe the infection state directly.\n\nWe will:\n\nsimulate a herd trajectory and imperfect test data,\nwrite the model with EpidemicTrajectories' rate functions,\nsample the parameters (with NUTS) and the hidden trajectory (with an iFFBS kernel) jointly, and\nplot the posterior against the values we simulated from.\n\nusing EpidemicTrajectories\nusing PracticalBayes\nusing Distributions\nusing Random\nusing AdvancedHMC: NUTS\nimport AbstractMCMC\nusing StableRNGs: StableRNG\nusing Statistics: mean\nusing FlexiChains\nusing CairoMakie","title":"Fitting a latent-trajectory epidemic model"},{"category":"section","location":"tutorials/cattle_iffbs/#Simulate-the-data","page":"Fitting a model with a PPL","text":"First we simulate a herd of ten pens of eight animals over eighty days, from a known set of parameters: the external and within-pen forces of infection alpha and beta, the mean infectious period m, the initial infection frequency nu, and the test sensitivity theta.\n\ntrue_pars = (; α = 0.01, β = 0.02, m = 6.0)\ntrue_ν = 0.10\ntrue_θ = 0.80\n\nn_pens, n_per_pen, n_times = 10, 8, 80\nn_ind = n_pens * n_per_pen\ngroup = repeat(1:n_pens; inner = n_per_pen)\n\nss = SI\nrates = TwoStateSI()\n\nrng = StableRNG(2024)\nstates, data = simulate_trajectory(rng, ss, rates, true_pars, group,\n                                   [1 - true_ν, true_ν]; n_times = n_times)\n\nWe apply an imperfect test (sensitivity theta, perfect specificity) on every sixth day, and mark the other days as unobserved with -1.\n\nrams = DiagnosticTest(; sensitivity = p -> p.θ, specificity = p -> 1.0, positive_code = 1)\nR = simulate_observations(rng, (rams,), (; θ = true_θ), ss, states)[1]\n\nobserved_days = 1:6:n_times\nRmask = fill(-1, size(R))\nRmask[:, observed_days] .= R[:, observed_days]","title":"Simulate the data"},{"category":"section","location":"tutorials/cattle_iffbs/#Bundle-the-model-and-data","page":"Fitting a model with a PPL","text":"EpidemicTrajectories works in three parts: model (the parameters), X (the latent trajectory), and data (the observations plus the group structure). epidemic_model captures the fixed structure — the state space, the rates, the tests — and returns loglik(model, X, data), simulate(rng, model, data), and latent!(rng, model, X, data). build_data bundles the observations and group membership once.\n\nem = epidemic_model(ss, rates; tests = (rams,))\ndata = build_data(em, group; observations = (Rmask,))","title":"Bundle the model and data"},{"category":"section","location":"tutorials/cattle_iffbs/#Reserve-the-hidden-trajectory-as-a-latent-variable","page":"Fitting a model with a PPL","text":"The hidden state matrix X (animals × time) is a latent variable the sampler fills in. We represent it as a matrix-valued distribution so PracticalBayes treats it as one discrete latent block, updated by our own kernel rather than by NUTS. Its density comes from em.loglik in the model body, so the distribution itself contributes nothing.\n\nstruct TrajectoryLatent <: Distributions.DiscreteMatrixDistribution\n    n_ind::Int\n    n_times::Int\nend\nBase.size(d::TrajectoryLatent) = (d.n_ind, d.n_times)\nDistributions.logpdf(::TrajectoryLatent, X::AbstractMatrix) = 0.0\nDistributions.rand(rng::Random.AbstractRNG, d::TrajectoryLatent) = zeros(Int, d.n_ind, d.n_times)","title":"Reserve the hidden trajectory as a latent variable"},{"category":"section","location":"tutorials/cattle_iffbs/#The-iFFBS-latent-kernel","page":"Fitting a model with a PPL","text":"The kernel resamples the whole hidden trajectory once per Gibbs sweep with a single call to em.latent!.\n\nstruct iFFBSKernel <: PracticalBayes.AbstractLatentKernel end\nfunction PracticalBayes.latent_step(rng, ::iFFBSKernel, block_names, c::ModelConditional)\n    pars = (; α = c.values.α, β = c.values.β, m = c.values.m̃ + 1.0, θ = c.values.θ)\n    X = copy(c.values.X)\n    em.latent!(rng, pars, X, data; initial_prob = [1 - c.values.ν, c.values.ν])\n    return (; X = X)\nend\n\nThe initial infection frequency nu and the test sensitivity theta have closed-form conditional posteriors, so we update them with small conjugate kernels rather than NUTS.\n\nstruct NuKernel <: PracticalBayes.AbstractLatentKernel end\nfunction PracticalBayes.latent_step(rng, ::NuKernel, block_names, c::ModelConditional)\n    x1 = @view c.values.X[:, 1]\n    n_inf = count(==(1), x1)\n    return (; ν = rand(rng, Beta(1 + n_inf, 1 + length(x1) - n_inf)))\nend\n\nstruct ThetaKernel <: PracticalBayes.AbstractLatentKernel\n    results::Matrix{Int}\nend\nfunction PracticalBayes.latent_step(rng, k::ThetaKernel, block_names, c::ModelConditional)\n    X, R = c.values.X, k.results\n    n_pos = n_inf = 0\n    for t in axes(R, 2), i in axes(R, 1)\n        (R[i, t] < 0 || X[i, t] != 1) && continue\n        n_inf += 1\n        R[i, t] == 1 && (n_pos += 1)\n    end\n    return (; θ = rand(rng, Beta(1 + n_pos, 1 + n_inf - n_pos)))\nend","title":"The iFFBS latent kernel"},{"category":"section","location":"tutorials/cattle_iffbs/#The-model","page":"Fitting a model with a PPL","text":"The parameters get priors; the hidden trajectory is the latent variable; and the trajectory log-likelihood is added with @addlogprob! em.loglik(pars, X). We reparameterise the infectious period as m = tilde m + 1 so the recovery probability 1m stays below one.\n\n@model function cattle_model(em, data, n_ind, n_times)\n    α ~ Gamma(1, 1)\n    β ~ Gamma(1, 1)\n    m̃ ~ Gamma(2, 4)\n    m := m̃ + 1.0\n    ν ~ Beta(1, 1)\n    θ ~ Beta(1, 1)\n\n    X ~ TrajectoryLatent(n_ind, n_times)\n    @addlogprob! em.loglik((; α = α, β = β, m = m, θ = θ), X, data)\nend","title":"The model"},{"category":"section","location":"tutorials/cattle_iffbs/#Sample","page":"Fitting a model with a PPL","text":"We assign each variable to a block: NUTS for the continuous transmission parameters, the conjugate kernels for nu and theta, and the iFFBS kernel for the hidden trajectory.\n\nm = cattle_model(em, data, n_ind, n_times)\n\nspl = Gibbs(\n    (:α, :β, :m̃) => NUTS(0.8),\n    :ν => NuKernel(),\n    :θ => ThetaKernel(Rmask),\n    :X => iFFBSKernel(),\n)\n\nWe start the hidden trajectory from a plausible simulation, then sample runs the sweeps and returns a chain; discard_initial drops burn-in.\n\nX0, _ = simulate_trajectory(StableRNG(99), ss, rates, true_pars, group,\n                            [1 - true_ν, true_ν]; n_times = n_times)\ninit = (; X = copy(X0), α = 0.05, β = 0.05, m̃ = 4.0, ν = 0.1, θ = 0.7)\n\nchn = AbstractMCMC.sample(StableRNG(7), m, spl, 1000;\n                          init = init, n_adapts = 400, discard_initial = 500)","title":"Sample"},{"category":"section","location":"tutorials/cattle_iffbs/#Check-the-recovery","page":"Fitting a model with a PPL","text":"We compare the posterior means to the values we simulated from. The infectious period is m = tilde m + 1.\n\nposterior_m = vec(chn[:m̃]) .+ 1.0\nrecovered = (α = vec(chn[:α]), β = vec(chn[:β]), m = posterior_m,\n             ν = vec(chn[:ν]), θ = vec(chn[:θ]))\ntruths = (α = true_pars.α, β = true_pars.β, m = true_pars.m, ν = true_ν, θ = true_θ)\n\nfor name in (:α, :β, :m, :ν, :θ)\n    println(rpad(name, 3), \" mean \", round(mean(getfield(recovered, name)); digits = 4),\n            \"  (truth \", getfield(truths, name), \")\")\nend","title":"Check the recovery"},{"category":"section","location":"tutorials/cattle_iffbs/#Plot","page":"Fitting a model with a PPL","text":"We plot each parameter's posterior density with a line marking the value we simulated from.\n\nfig = Figure(size = (900, 500))\nfor (i, name) in enumerate((:α, :β, :m, :ν, :θ))\n    ax = Axis(fig[fldmod1(i, 3)...]; title = string(name), ylabel = \"density\")\n    density!(ax, getfield(recovered, name))\n    vlines!(ax, [getfield(truths, name)]; color = :firebrick, linewidth = 2)\nend\nfig\n\nEach posterior concentrates around the value we simulated from — the sampler has recovered the transmission parameters, the infectious period, the initial infection frequency, and the test sensitivity, all while inferring the hidden infection trajectory it never observed directly.\n\n\n\nThis page was generated using Literate.jl.","title":"Plot"},{"category":"section","location":"#EpidemicTrajectories.jl","page":"Home","text":"Discrete-time, individual-level epidemic models, built from three pieces:\n\na simulator that draws hidden state trajectories forward in time,\nan automatic-differentiation-friendly likelihood of the model parameters given a state trajectory and observed data, and\na latent-state sampler — individual forward-filtering, backward-sampling (iFFBS) — that draws a new trajectory given the current parameters.\n\nEach is an ordinary Julia function with no dependency on a probabilistic programming framework, so the likelihood can be used directly as an HMC target and the iFFBS sampler as a Gibbs latent-variable update.","title":"EpidemicTrajectories.jl"},{"category":"section","location":"#Two-ways-to-specify-a-model","page":"Home","text":"Both produce the same per-step transition probabilities and share the simulator, likelihood, and iFFBS machinery.\n\nRate functions. Supply the per-step transition probabilities as functions of the parameters, the individual, and the time: f(pars, model, data, i, t). This form supports per-individual covariates, individual-specific observation models, network or spatial forces of infection, and history-dependent rates. TwoStateSI is the built-in bundle for the two-state model.\nState transitions. List the allowed (from, to) transitions and give each a rate, with the @transitions macro. SimpleEpiTransitionMatrix takes rates that depend only on compartment counts (a chain-binomial model); EpiTransitionMatrix takes the full per-individual rate signature.","title":"Two ways to specify a model"},{"category":"section","location":"#Getting-started","page":"Home","text":"The tutorial fits a two-state susceptible/infected model to simulated capture-recapture data with PracticalBayes.jl, recovering the transmission parameters, the infectious period, and the test sensitivity while inferring the hidden infection trajectory.","title":"Getting started"}]
}
