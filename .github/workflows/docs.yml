name: Docs

on:
  push:
    branches:
      - master
    paths:
      - 'docs/**'
      - 'src/**'
      - 'Project.toml'
      - '.github/workflows/docs.yml'
  pull_request:
    paths:
      - 'docs/**'
      - 'src/**'
      - 'Project.toml'
      - '.github/workflows/docs.yml'

jobs:
  build:
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
      statuses: write
    steps:
      - uses: actions/checkout@v4

      - uses: julia-actions/setup-julia@v2
        with:
          version: '1'

      - uses: julia-actions/cache@v2

      # PracticalBayes is not registered, so the docs environment adds it from
      # its GitHub repository (the tutorial fits a model with it) and develops
      # this package from the checkout. add/develop resolve the manifest, so no
      # separate instantiate is needed (and instantiate would try to resolve the
      # unregistered PracticalBayes from a registry and fail).
      - name: Install docs environment
        run: |
          julia --project=docs -e '
            using Pkg
            Pkg.add(PackageSpec(url="https://github.com/EvoArt/PracticalBayes.git"))
            Pkg.develop(PackageSpec(path=pwd()))'

      - name: Build and deploy docs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: julia --project=docs docs/make.jl
